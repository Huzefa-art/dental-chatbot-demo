// No secrets here -- this is a public, non-sensitive backend URL, safe to ship in a static file.
// Update this ONE line after you deploy the backend to Render (Step 4 in the deployment guide),
// then redeploy the frontend on Vercel (or just push to git if auto-deploy is on).
window.API_BASE_URL = "http://localhost:8000";
window.CLINIC_SLUG = "hiner_family_dentistry";
